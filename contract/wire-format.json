{
  "$comment": [
    "The wire format shared by the Bazaar Utils website and the Bazaar Utils mod.",
    "",
    "These values are duplicated by necessity: the website declares them as Zod",
    "schemas, the mod as Java records and constants, and neither can import the",
    "other. This file is the single written-down copy, and lib/minecraft/contract.test.ts",
    "asserts the website's schemas still agree with it. A change here is a protocol",
    "change and needs a matching change in mkram17/Bazaar-Utils.",
    "",
    "The mod's counterparts live in:",
    "  utils/web/OrderSnapshot.java        enums, string caps",
    "  utils/web/BazaarUtilsApi.java       normalizeLinkCode, request records",
    "  features/web/OrderSyncService.java  maxOrdersPerSync"
  ],
  "orderSync": {
    "$comment": [
      "pricingPosition used to be listed here. It is no longer sent: it is derived from",
      "market data that moves every ~20s, so any value the mod put on the wire described",
      "the market as of the last sync rather than now. The website reads the same public",
      "Hypixel endpoint and derives it per request. The mod still computes it locally for",
      "its own outbid chat alerts and inventory highlighting, which must work unlinked.",
      "",
      "The website still ACCEPTS the field, because installs in the wild keep sending it;",
      "it is parsed and ignored rather than rejected."
    ],
    "endpoint": "/api/orders/sync",
    "maxOrdersPerSync": 200,
    "sides": ["BUY", "SELL"],
    "statuses": ["SET", "FILLED"],
    "maxProductIdLength": 128,
    "maxItemNameLength": 128,
    "maxProfileIdLength": 64,
    "maxUsernameLength": 16,
    "usernamePattern": "^[A-Za-z0-9_]{1,16}$"
  },
  "linkCode": {
    "endpoint": "/api/link/confirm",
    "alphabet": "23456789ABCDEFGHJKMNPQRSTUVWXYZ",
    "length": 8,
    "maxSubmittedLength": 32,
    "$comment": [
      "normalizeLinkCode must agree character for character on both sides: the",
      "normalized code IS the serverId nonce the mod passes to joinServer and the",
      "website passes to hasJoined, so any divergence turns every link attempt into a",
      "silent verification failure rather than a visible error."
    ],
    "normalization": [
      { "input": "abcdefgh", "output": "ABCDEFGH" },
      { "input": "  ABCD1234  ", "output": "ABCD1234" },
      { "input": "ABCD-1234", "output": "ABCD1234" },
      { "input": "AB CD 12 34", "output": "ABCD1234" },
      { "input": "ab-cd 12-34", "output": "ABCD1234" },
      { "input": "", "output": "" }
    ]
  }
}
